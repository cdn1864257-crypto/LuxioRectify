# Configuration pour l'environnement de développement Replit
# Copiez ce fichier vers `.env` et ajustez les valeurs selon vos besoins

# Option 1: Utiliser le backend de production sur Render (recommandé pour Replit)
# Cela permet de tester le frontend sans configurer un backend local complet
VITE_API_URL=https://api.luxiomarket.shop

# Option 2: Utiliser le backend local (nécessite la configuration complète)
# VITE_API_URL=http://localhost:3001
# Dans ce cas, vous devez également configurer les secrets Replit suivants:
# - MONGODB_URI
# - JWT_SECRET  
# - ENCRYPTION_KEY
# - SENDGRID_API_KEY
# - SENDGRID_FROM_EMAIL
